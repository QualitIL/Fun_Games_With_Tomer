ID: Fun_Games_With_Tomer
_schema-version: '2.1'
version: 0.0.1

modules:
 

resources:
 - name: hdi_TEST1
   parameters:
      config:
         schema: GAMES
   properties:
      hdi-container-name: ${service-name}
   type: com.sap.xs.hdi-container
